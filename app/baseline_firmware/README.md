Place baseline Arduino #2 firmware here.

Expected layout:
- Provide a single .ino sketch (or a zip containing a single .ino).
- Example: baseline.ino

When present, the teacher can switch student mode to "baseline" and the server will attempt to compile/upload this firmware to STUDENT_PORT.
